太好了！我们现在进入 **1.5.3：默认参数 — 声明中指定、从右向左省略**。

这是 C++ 函数设计中一个**简洁而强大**的特性，允许你为函数参数提供默认值，从而**减少函数重载数量**、**提升 API 友好性**。但默认参数有严格的规则（如必须从右向左定义），且与 Java 的方法重载有本质差异。掌握它，能让你写出更优雅、更易用的 C++ 接口。

我们将继续严格遵循你的学习偏好：
- ✅ SMART 目标  
- ✅ 结构先行（默认参数地图）  
- ✅ 生活化比喻  
- ✅ 动手为王（含基础/进阶/陷阱题）  
- ✅ 螺旋复习（衔接 1.5.1–1.5.2 + Java 经验）  
- ✅ 主动学习 + 复盘

---

## 🎯 KU 1.5.3：默认参数

> **所属阶段**：Phase 1 — C++ 语法基础  
> **前置知识**：1.5.1–1.5.2（函数基础、参数传递）、Java 方法重载经验  
> **后续衔接**：1.5.4（函数重载）、2.2（构造函数）  
> **预计耗时**：2–3 小时

---

### 🧭 一、结构先行：默认参数地图

```
1.5.3 默认参数
├── 1. 什么是默认参数？
│   ├── 1.1 语法：T param = default_value
│   ├── 1.2 作用：简化函数调用
│   └── 1.3 与函数重载的关系
├── 2. 核心规则
│   ├── 2.1 必须在声明中指定（通常 .h）
│   ├── 2.2 从右向左连续定义（不能跳跃）
│   └── 2.3 默认值必须是编译期常量（或全局可见）
├── 3. 使用方式
│   ├── 3.1 调用时省略参数（从右开始）
│   └── 3.2 不能部分省略（中间参数）
├── 4. 与 Java 的对比
│   ├── 4.1 Java 无默认参数
│   └── 4.2 Java 用重载模拟
├── 5. 常见陷阱与最佳实践
    ├── 5.1 头文件 vs 源文件定义
    ├── 5.2 默认值修改的兼容性
    └── 5.3 现代 C++ 建议
```

---

### 🎯 二、SMART 学习目标

- **Specific**：能正确使用默认参数，理解其规则（右连续、声明中指定），并避免常见陷阱。
- **Measurable**：完成 2 道基础题 + 2 道陷阱题，能解释为何不能跳跃定义默认参数。
- **Achievable**：通过“餐厅点餐”比喻理解默认参数。
- **Relevant**：这是设计友好 API 的重要工具。
- **Time-bound**：1–2 天内完成。

---

### 🌍 三、生活化比喻：餐厅点餐选项

> 想象你在点一份**定制汉堡**：
>
> - **菜单（函数声明）**：  
>   “汉堡（面包类型, 肉饼数量, 酱料, 生菜）”  
>   - 默认：汉堡（"芝麻", 1, "番茄酱", true）
>
> - **点餐（函数调用）**：  
>   - `burger()` → 全默认  
>   - `burger("全麦")` → 面包=全麦，其余默认  
>   - `burger("全麦", 2)` → 面包=全麦，肉饼=2，其余默认  
>   - **不能**：`burger(, , "芥末")`（跳过前面参数）
>
> - **Java 对比**：  
>   - Java 餐厅需提供多个菜单：  
>     `burger()`, `burger(bread)`, `burger(bread, patties)`...  
>   - **C++ 餐厅只需一个菜单，带默认选项**

> 💡 **关键认知**：**默认参数必须从右向左连续，不能跳跃**。

---

### 📚 四、知识点分解

#### 1. 什么是默认参数？
- **语法**：
  ```cpp
  void connect(std::string host, int port = 80, bool ssl = false);
  ```
- **作用**：
  - 减少函数重载数量
  - 提供灵活的 API（常用参数设默认）
- **与重载关系**：
  - 默认参数是**语法糖**，编译器生成多个调用形式
  - 但**不能与重载函数冲突**

#### 2. 核心规则
- **2.1 声明中指定**：
  - 默认值必须在**函数声明**中指定（通常在 .h）
  - 定义中**不能重复默认值**：
    ```cpp
    // math.h
    int add(int a, int b = 0); // 正确：声明中指定

    // math.cpp
    int add(int a, int b /* = 0 */) { // 正确：定义中省略默认值
        return a + b;
    }
    ```
- **2.2 从右向左连续**：
  ```cpp
  // 合法：右连续
  void f(int a, int b = 1, int c = 2);

  // 非法：跳跃
  void g(int a = 0, int b, int c = 2); // 错误！b 无默认值
  ```
- **2.3 默认值要求**：
  - 必须是**编译期常量**或**全局可见名称**
  - 不能是局部变量：
    ```cpp
    void bad() {
        int x = 42;
        void f(int a = x); // 错误！x 不是全局可见
    }
    ```

#### 3. 使用方式
- **调用时省略**（从右开始）：
  ```cpp
  connect("example.com");           // port=80, ssl=false
  connect("example.com", 443);      // ssl=false
  connect("example.com", 443, true); // 全指定
  ```
- **不能部分省略**：
  ```cpp
  // connect("example.com", , true); // 非法！不能跳过 port
  ```

#### 4. 与 Java 的对比
| 特性 | C++ | Java |
|------|-----|------|
| **默认参数** | ✅ 支持 | ❌ 不支持 |
| **模拟方式** | 直接语法 | 方法重载 |
| **API 简洁性** | 高（一个函数） | 低（多个重载） |
| **二进制兼容** | 修改默认值需重编译调用者 | 重载增减需重编译 |

#### 5. 常见陷阱
- **头文件定义**：
  - 默认值在 .h 中，修改后**所有包含者需重编译**
- **与重载冲突**：
  ```cpp
  void f(int a, int b = 0);
  void f(int a); // 错误！调用 f(1) 有歧义
  ```
- **虚函数默认参数**：
  - 默认值由**静态类型**决定，非动态类型（易错！）：
    ```cpp
    class Base {
    public:
        virtual void f(int x = 10) { std::cout << x << "\n"; }
    };
    class Derived : public Base {
    public:
        void f(int x = 20) override { std::cout << x << "\n"; }
    };
    Base* p = new Derived();
    p->f(); // 输出 10！（Base 的默认值）
    ```

> 💡 **现代 C++ 建议**：
> - **默认参数**：用于简化常用场景
> - **复杂默认**：用工厂函数或 Builder 模式
> - **虚函数**：避免默认参数（用重载替代）

---

### 💻 五、动手为王：可运行示例

#### 示例 1：基本默认参数
```cpp
// defaults.h
#ifndef DEFAULTS_H
#define DEFAULTS_H
#include <iostream>
#include <string>

void greet(const std::string& name, 
           const std::string& greeting = "Hello", 
           bool loud = false);
#endif

// defaults.cpp
#include "defaults.h"
void greet(const std::string& name, 
           const std::string& greeting, 
           bool loud) {
    std::string msg = greeting + ", " + name + "!";
    if (loud) {
        // 转大写（简化）
        for (char& c : msg) c = std::toupper(c);
    }
    std::cout << msg << "\n";
}

// main.cpp
#include "defaults.h"
int main() {
    greet("Alice");                    // Hello, Alice!
    greet("Bob", "Hi");               // Hi, Bob!
    greet("Charlie", "Hey", true);    // HEY, CHARLIE!
    return 0;
}
```

#### 示例 2：非法跳跃默认
```cpp
// illegal.cpp
void bad_func(int a = 1, int b, int c = 3); // 编译错误！

int main() {
    return 0;
}
```
> ❌ 输出：  
> `error: default argument missing for parameter 2`

#### 示例 3：虚函数默认参数陷阱
```cpp
#include <iostream>

class Base {
public:
    virtual void log(int level = 1) {
        std::cout << "Base: " << level << "\n";
    }
};

class Derived : public Base {
public:
    void log(int level = 2) override {
        std::cout << "Derived: " << level << "\n";
    }
};

int main() {
    Derived d;
    d.log(); // Derived: 2

    Base* p = &d;
    p->log(); // Base: 1！（静态类型 Base 决定默认值）
    return 0;
}
```

---

### 📝 六、分层练习题

#### 🔹 基础题
1. **合法默认参数**  
   - 判断以下声明是否合法：
     ```cpp
     void f(int a, int b = 1, int c = 2);
     void g(int a = 0, int b = 1, int c);
     ```

2. **函数调用**  
   - 给定 `void connect(std::string host, int port = 80, bool ssl = false);`  
     - 写出调用 `connect` 仅指定 `host` 和 `ssl=true` 的方式

#### 🔸 进阶题
3. **头文件实践**  
   - 在 `utils.h` 中声明带默认参数的函数，在 `utils.cpp` 中定义

4. **虚函数陷阱**  
   - 解释为何 `p->log()` 输出 `Base: 1`

#### ⚠️ 陷阱题（Java 迁移误区）
5. **“Java 用重载，C++ 用默认参数，一样吧？”**  
   - Q: 默认参数和函数重载可以混用吗？  
   - A: ⚠️ **谨慎混用**！  
     → 可能导致调用歧义：
     ```cpp
     void f(int a, int b = 0);
     void f(int a); // f(1) 有歧义！
     ```

6. **“默认值可以是局部变量吧？”**  
   - Q: 以下代码合法吗？
     ```cpp
     void outer() {
         int x = 42;
         void inner(int a = x); // ?
     }
     ```
   - A: ❌ **非法**！  
     → 默认值必须在**全局作用域可见**

---

### ⚠️ 七、注意事项与建议

| 问题 | 建议 |
|------|------|
| **默认值位置** | 只在 .h 声明中指定 |
| **虚函数** | 避免默认参数，用重载替代 |
| **二进制兼容** | 修改默认值需重编译所有调用者 |
| **API 设计** | 默认值应为最常用场景 |
| **现代替代** | 复杂场景用 `std::optional`（Phase 5）或 Builder 模式 |

> 💡 **现代 C++ 建议**：
> - **简单默认**：用默认参数
> - **可选语义**：用 `std::optional<T>`（明确表示“可能无值”）
> - **虚函数**：不要用默认参数

---

### 🔁 八、螺旋复习

- **衔接 1.5.1**：默认参数在声明中指定（.h）
- **衔接 1.5.4**：默认参数 vs 函数重载
- **Java 经验**：C++ 默认参数减少重载数量
- **当日复习**：默写默认参数三大规则

---

### 🧠 九、主动学习任务

1. **自我提问**：
   - “为什么默认参数必须从右向左？”
   - “默认参数在编译期还是运行期求值？”
2. **概念图**：画出默认参数与函数重载的对比
3. **扮演教师**：向同事解释“为什么虚函数默认参数是陷阱”

---

### 🔍 十、复盘清单

✅ 我能正确使用默认参数  
✅ 我理解默认参数必须右连续  
✅ 我知道默认值在声明中指定  
✅ 我能避免虚函数默认参数陷阱  
✅ 我明白 C++ 与 Java 在参数默认上的差异

> 🎯 **达标标准**：完成基础题 + 正确分析陷阱题 5 和 6

---

## 🚀 下一步建议

你已掌握 **C++ 默认参数的核心知识**！

接下来你可以：
1. **继续学习 1.5.4 函数重载：基于参数类型/数量的重载决议**
2. **请求生成「默认参数安全使用速查卡」**
3. **跳转到练习题答案与解析**

请告诉我你的选择，我们将继续前行！