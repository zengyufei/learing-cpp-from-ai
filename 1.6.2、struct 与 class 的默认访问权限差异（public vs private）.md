太好了！我们现在进入 **1.6.2：struct 与 class 的默认访问权限差异（public vs private）**。

这是 C++ 面向对象设计中一个**看似微小却影响深远**的语法差异。作为 Java 开发者，你习惯用 `class` 并通过 `private`/`public` 显式控制访问，而 C++ 的 `struct` 和 `class` **几乎完全等价**，唯一的区别就是**默认访问权限**。理解这一点，能帮助你在设计数据聚合（struct）与封装对象（class）时做出更符合 C++ 习惯的选择。

我们将继续严格遵循你的学习偏好：
- ✅ SMART 目标  
- ✅ 结构先行（访问控制地图）  
- ✅ 生活化比喻  
- ✅ 动手为王（含基础/进阶/陷阱题）  
- ✅ 螺旋复习（衔接 1.6.1 + Java 经验）  
- ✅ 主动学习 + 复盘

---

## 🎯 KU 1.6.2：struct 与 class 默认访问权限差异

> **所属阶段**：Phase 1 — C++ 语法基础  
> **前置知识**：1.6.1（struct 基础）、Java class 访问控制经验  
> **后续衔接**：2.1（类与对象）、2.9（静态成员）  
> **预计耗时**：2–3 小时

---

### 🧭 一、结构先行：访问控制地图

```
1.6.2 struct vs class 访问权限
├── 1. 核心差异
│   ├── 1.1 struct：默认 public
│   └── 1.2 class：默认 private
├── 2. 详细规则
│   ├── 2.1 成员访问默认权限
│   ├── 2.2 继承默认权限
│   └── 2.3 显式指定可覆盖默认
├── 3. 语义与惯例
│   ├── 3.1 struct：数据聚合（POD）
│   └── 3.2 class：封装对象（行为隐藏）
├── 4. 与 Java 的对比
│   ├── 4.1 Java 只有 class
│   └── 4.2 Java 默认 package-private
├── 5. 常见陷阱与最佳实践
    ├── 5.1 混淆默认权限导致编译错误
    ├── 5.2 继承时的默认权限陷阱
    └── 5.3 现代 C++ 设计指南
```

---

### 🎯 二、SMART 学习目标

- **Specific**：能解释 `struct` 与 `class` 的默认访问权限差异，理解其在成员和继承中的表现，并遵循 C++ 设计惯例。
- **Measurable**：完成 2 道基础题 + 2 道陷阱题，能修复因默认权限导致的编译错误。
- **Achievable**：通过“透明盒子 vs 封装盒子”比喻理解访问控制。
- **Relevant**：这是编写符合 C++ 习惯的面向对象代码的基础。
- **Time-bound**：1–2 天内完成。

---

### 🌍 三、生活化比喻：透明盒子 vs 封装盒子

> 想象两种产品包装：
>
> - **struct = 透明盒子**  
>   - 所有内容**默认可见**（public）  
>   - 适合展示简单物品（如水果、文具）  
>   - **C++ 惯例**：用于数据聚合
>
> - **class = 封装盒子**  
>   - 内容**默认隐藏**（private）  
>   - 通过小窗口（public 方法）交互  
>   - **C++ 惯例**：用于封装复杂行为
>
> - **Java 对比**：  
>   - Java 只有“封装盒子”（class）  
>   - **C++ 提供两种盒子**，让你根据场景选择

> 💡 **关键认知**：**`struct` 和 `class` 功能等价，差异仅在默认权限和设计语义**。

---

### 📚 四、知识点分解

#### 1. 核心差异
| 类型 | 默认成员访问权限 | 默认继承权限 |
|------|-----------------|------------|
| `struct` | **public** | **public** |
| `class` | **private** | **private** |

> ✅ **功能完全等价**！以下两段代码行为相同：
> ```cpp
> struct Point {
>     double x, y; // public by default
> };
> 
> class Point {
> public:          // 显式 public
>     double x, y;
> };
> ```

#### 2. 详细规则
- **成员访问**：
  ```cpp
  struct S {
      int a;    // public
  private:
      int b;    // private
  };
  
  class C {
      int a;    // private
  public:
      int b;    // public
  };
  ```
- **继承权限**：
  ```cpp
  struct Base {};
  
  struct Derived : Base {};      // public inheritance
  class Derived : Base {};       // private inheritance
  
  // 等价于：
  struct Derived : public Base {};
  class Derived : private Base {};
  ```
- **显式指定覆盖默认**：
  - 无论 `struct`/`class`，都可用 `public`/`private`/`protected` 显式控制

#### 3. 语义与惯例
- **`struct` 惯例**：
  - 数据为主，行为为辅
  - 成员通常 public（如 `Point`, `Config`）
  - POD（Plain Old Data）类型
- **`class` 惯例**：
  - 行为为主，数据隐藏
  - 成员通常 private，通过 public 方法访问
  - 封装资源（如 `FileHandle`, `Mutex`）

#### 4. 与 Java 的对比
| 特性 | C++ | Java |
|------|-----|------|
| **类型关键字** | `struct` 和 `class` | 只有 `class` |
| **默认访问** | struct: public<br>class: private | package-private（无修饰符） |
| **设计语义** | struct = 数据<br>class = 封装 | class = 通用（靠修饰符控制） |
| **继承默认** | struct: public<br>class: private | public（无修饰符） |

#### 5. 常见陷阱
- **成员访问错误**：
  ```cpp
  class Point {
      double x, y; // private by default
  };
  int main() {
      Point p;
      p.x = 1.0; // 编译错误！x is private
  }
  ```
  ✅ 修复：加 `public:` 或改用 `struct`
- **继承权限错误**：
  ```cpp
  class Base {
  public:
      void f();
  };
  class Derived : Base {}; // private inheritance
  int main() {
      Derived d;
      d.f(); // 错误！f is private in Derived
  }
  ```
  ✅ 修复：`class Derived : public Base {}`

> 💡 **现代 C++ 建议**：
> - **数据聚合** → `struct`（默认 public）
> - **封装对象** → `class`（默认 private）
> - **继承** → 显式写 `public`/`private`（避免依赖默认）

---

### 💻 五、动手为王：可运行示例

#### 示例 1：默认权限对比
```cpp
#include <iostream>

// struct: 默认 public
struct PointS {
    double x, y;
};

// class: 默认 private
class PointC {
    double x, y; // private
public:
    void set(double x_, double y_) { x = x_; y = y_; }
    void print() const { std::cout << "(" << x << "," << y << ")\n"; }
};

int main() {
    PointS ps{1.0, 2.0};
    std::cout << "PointS: (" << ps.x << "," << ps.y << ")\n"; // OK

    PointC pc;
    // pc.x = 1.0; // 错误！x is private
    pc.set(1.0, 2.0);
    pc.print(); // OK
    return 0;
}
```

#### 示例 2：继承默认权限
```cpp
#include <iostream>

struct BaseS {
    void f() { std::cout << "BaseS::f\n"; }
};

class BaseC {
public:
    void f() { std::cout << "BaseC::f\n"; }
};

// struct 继承：默认 public
struct DerivedS : BaseS {};

// class 继承：默认 private
class DerivedC : BaseC {};

int main() {
    DerivedS ds;
    ds.f(); // OK (public inheritance)

    DerivedC dc;
    // dc.f(); // 错误！private inheritance
    return 0;
}
```

#### 示例 3：显式覆盖默认
```cpp
#include <iostream>

// 用 class 实现 public 数据（不推荐，但合法）
class PublicPoint {
public:
    double x, y;
};

// 用 struct 实现 private 数据（不推荐，但合法）
struct PrivateConfig {
private:
    std::string secret;
public:
    void set_secret(const std::string& s) { secret = s; }
};

int main() {
    PublicPoint p{1, 2};
    std::cout << p.x << "\n"; // OK

    PrivateConfig c;
    c.set_secret("hidden");
    // c.secret = "x"; // 错误！
    return 0;
}
```

---

### 📝 六、分层练习题

#### 🔹 基础题
1. **默认权限识别**  
   - 判断以下成员的访问权限：
     ```cpp
     struct S { int a; };
     class C { int b; };
     ```

2. **继承权限**  
   - `struct D : B {};` 的继承权限是什么？  
   - `class D : B {};` 的继承权限是什么？

#### 🔸 进阶题
3. **修复编译错误**  
   - 修复以下代码：
     ```cpp
     class Point {
         double x, y;
     };
     int main() {
         Point p;
         p.x = 1.0; // 错误
     }
     ```

4. **设计选择**  
   - 为以下场景选择 `struct` 或 `class`：  
     a) 2D 点坐标  
     b) 文件读写器  
     c) HTTP 请求配置

#### ⚠️ 陷阱题（Java 迁移误区）
5. **“class 成员默认 public 吧？”**  
   - Q: C++ 中 `class Point { double x; };` 的 `x` 是 public 吗？  
   - A: ❌ **不是**！  
     → `class` 默认 **private**，`struct` 默认 **public**

6. **“继承默认都是 public 吧？”**  
   - Q: `class Derived : Base {};` 是 public 继承吗？  
   - A: ❌ **不是**！  
     → `class` 继承默认 **private**，`struct` 默认 **public**

---

### ⚠️ 七、注意事项与建议

| 问题 | 建议 |
|------|------|
| **设计语义** | struct 用于数据，class 用于封装 |
| **继承** | 显式写 `public`/`private`（避免默认） |
| **一致性** | 团队统一风格（如 Google C++ Style） |
| **POD 类型** | 用 struct 保证 trivial layout |
| **现代实践** | 优先按语义选择，而非功能（因功能等价）

> 💡 **现代 C++ 黄金法则**：  
> **“数据用 struct（默认 public），封装用 class（默认 private）；继承显式写权限”**

---

### 🔁 八、螺旋复习

- **衔接 1.6.1**：struct 可包含函数，但默认 public
- **衔接 2.1**：class 是面向对象的核心
- **Java 经验**：C++ 提供两种类型关键字，Java 只有一种
- **当日复习**：默写 struct 与 class 的默认权限表

---

### 🧠 九、主动学习任务

1. **自我提问**：
   - “为什么 C++ 要保留 struct 和 class 两种关键字？”
   - “能否用 class 完全替代 struct？”
2. **概念图**：画出 struct 与 class 的默认权限对比
3. **扮演教师**：向同事解释“为什么 `class` 成员默认 private”

---

### 🔍 十、复盘清单

✅ 我能说出 struct 与 class 的默认权限差异  
✅ 我理解默认继承权限的不同  
✅ 我会按语义选择 struct 或 class  
✅ 我能修复因默认权限导致的错误  
✅ 我明白 C++ 与 Java 在类型设计上的差异

> 🎯 **达标标准**：完成基础题 + 正确分析陷阱题 5 和 6

---

## 🚀 下一步建议

你已掌握 **C++ struct 与 class 的核心差异**！

接下来你可以：
1. **继续学习 1.6.3 成员函数在 struct 中的定义（初步）**
2. **请求生成「C++ 访问控制速查卡」**
3. **跳转到练习题答案与解析**

请告诉我你的选择，我们将继续前行！