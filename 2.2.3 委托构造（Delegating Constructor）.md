太好的！我们现在进入 **2.2.3：委托构造（Delegating Constructor）**。

这是 C++11 带来的**代码复用革命**，彻底解决了 C++ 中**多个构造函数重复代码**的痛点。作为 Java 开发者，你熟悉 `this()` 构造器链式调用，而 C++ 的委托构造提供了**几乎相同**的功能，但有其独特的语法规则和限制。掌握委托构造，能让你编写出更简洁、更易维护的 C++ 构造函数。

我们将继续严格遵循你的学习偏好：
- ✅ SMART 目标  
- ✅ 结构先行（委托构造地图）  
- ✅ 生活化比喻  
- ✅ 动手为王（含基础/进阶/陷阱题）  
- ✅ 螺旋复习（衔接 2.2.1–2.2.2 + Java 经验）  
- ✅ 主动 learning + 复盘

---

## 🎯 KU 2.2.3：委托构造

> **所属阶段**：Phase 2 — 面向对象编程  
> **前置知识**：2.2.1–2.2.2（构造函数基础）、Java 构造器链经验  
> **后续衔接**：2.2.4（默认/删除构造函数）、2.2.5（explicit 关键字）  
> **预计耗时**：2–3 小时

---

### 🧭 一、结构先行：委托构造地图

```
2.2.3 委托构造
├── 1. 基本语法
│   └── 1.1 构造函数初始化列表中调用其他构造函数
├── 2. 核心规则
│   ├── 2.1 委托必须在初始化列表中（不能在函数体）
│   ├── 2.2 被委托构造函数先执行
│   └── 2.3 委托后本体函数体仍执行
├── 3. 与 Java 的对比
│   ├── 3.1 C++: MyClass() : MyClass(0) {}
│   └── 3.2 Java: MyClass() { this(0); }
├── 4. 常见用途
│   ├── 4.1 默认参数模拟
│   ├── 4.2 减少重复代码
│   └── 4.3 聚合初始化辅助
├── 5. 陷阱与限制
    ├── 5.1 不能委托给自身（无限递归）
    ├── 5.2 不能同时初始化成员和委托
    └── 5.3 虚函数调用在委托中仍危险
└── 6. 最佳实践
    ├── 6.1 优先委托而非复制代码
    ├── 6.2 明确委托链
    └── 6.3 避免深层委托
```

---

### 🎯 二、SMART 学习目标

- **Specific**：能正确使用委托构造，理解其执行顺序和限制，并与 Java 构造器链对比。
- **Measurable**：完成 2 道基础题 + 2 道陷阱题，能解释为何委托必须在初始化列表中。
- **Achievable**：通过“主厨与学徒”比喻理解委托。
- **Relevant**：这是编写简洁 C++ 构造函数的标准方式。
- **Time-bound**：1–2 天内完成。

---

### 🌍 三、生活化比喻：主厨与学徒

> 想象厨房中的**主厨与学徒**：
>
> - **普通构造函数 = 学徒独立做菜**  
>   - 需重复切菜、调味等步骤
>
> - **委托构造 = 学徒请主厨先做基础**  
>   - 学徒说：“主厨，先按您的标准做法做 base”  
>   - **主厨做完**，学徒再加自己的特色
>
> - **Java 对比**：  
>   - Java = 学徒先自己做 base，再叫主厨改进  
>   - **C++ = 主厨先做 base，学徒再加工**  
>   - **C++ 更高效**（避免重复劳动）

> 💡 **关键认知**：**委托构造 = 先执行被委托函数，再执行本体**。

---

### 📚 四、知识点分解

#### 1. 基本语法
- **委托调用**：
  ```cpp
  class MyClass {
      int value_;
  public:
      // 主构造函数
      MyClass(int value) : value_(value) {}
      
      // 委托构造函数
      MyClass() : MyClass(42) {} // 委托给 MyClass(int)
  };
  ```

#### 2. 核心规则
- **必须在初始化列表中**：
  ```cpp
  MyClass() {
      MyClass(42); // 错误！不能在函数体委托
  }
  ```
- **执行顺序**：
  1. 被委托构造函数的初始化列表
  2. 被委托构造函数的函数体
  3. 本体构造函数的函数体
- **本体函数体仍执行**：
  ```cpp
  MyClass() : MyClass(42) {
      std::cout << "Default constructor body\n";
  }
  // 输出：先 MyClass(42) 的内容，再本体内容
  ```

#### 3. 与 Java 的对比
| 特性 | C++ 委托构造 | Java 构造器链 |
|------|-------------|--------------|
| **语法** | `MyClass() : MyClass(42) {}` | `MyClass() { this(42); }` |
| **位置** | 初始化列表 | 函数体第一行 |
| **执行** | 被委托 → 本体 | 被调用 → 本体 |
| **限制** | 不能同时初始化成员 | 不能有其他语句 before `this()` |

#### 4. 常见用途
- **默认参数模拟**：
  ```cpp
  // 替代多个重载
  Point() : Point(0.0, 0.0) {}
  Point(double x) : Point(x, 0.0) {}
  Point(double x, double y) : x_(x), y_(y) {}
  ```
- **减少重复**：
  ```cpp
  // 避免在多个构造函数中重复初始化逻辑
  Logger() : Logger("default.log") {}
  Logger(const std::string& file) : file_(file) { /* open file */ }
  ```

#### 5. 陷阱与限制
- **无限递归**：
  ```cpp
  MyClass() : MyClass() {} // 错误！无限递归
  ```
- **不能混合初始化**：
  ```cpp
  MyClass(int x) : MyClass(), x_(x) {} // 错误！不能同时委托和初始化成员
  ```
- **虚函数危险**：
  ```cpp
  class Base {
  public:
      Base() { foo(); } // 危险！
      virtual void foo() { std::cout << "Base\n"; }
  };
  
  class Derived : public Base {
  public:
      Derived() : Base() {} // 调用 Base::foo()，非 Derived::foo()
      void foo() override { std::cout << "Derived\n"; }
  };
  ```

#### 6. 最佳实践
- **优先委托**：
  - 减少代码重复
- **明确委托链**：
  - 主构造函数处理核心逻辑
  - 其他委托给它
- **避免深层委托**：
  - 保持委托链浅（≤2 层）

> 💡 **现代 C++ 黄金法则**：  
> **“委托构造是代码复用利器；初始化列表中委托；避免混合初始化”**

---

### 💻 五、动手为王：可运行示例

#### 示例 1：基本委托
```cpp
#include <iostream>

class Point {
    double x_, y_;
public:
    // 主构造函数
    Point(double x, double y) : x_(x), y_(y) {
        std::cout << "Main constructor\n";
    }
    
    // 委托构造
    Point() : Point(0.0, 0.0) {
        std::cout << "Default constructor\n";
    }
    
    Point(double x) : Point(x, 0.0) {
        std::cout << "Single param constructor\n";
    }
    
    void print() const {
        std::cout << "(" << x_ << ", " << y_ << ")\n";
    }
};

int main() {
    Point p1;          // 调用 Point() → Point(0,0)
    p1.print();
    
    Point p2(3.0);     // 调用 Point(3) → Point(3,0)
    p2.print();
    
    Point p3(1.0, 2.0); // 直接调用主构造
    p3.print();
    
    return 0;
}
```
> ✅ 输出：
> ```
> Main constructor
> Default constructor
> (0, 0)
> Main constructor
> Single param constructor
> (3, 0)
> Main constructor
> (1, 2)
> ```

#### 示例 2：Java 对比
```java
// Java Constructor Chaining
public class Point {
    private double x, y;
    
    public Point(double x, double y) {
        this.x = x;
        this.y = y;
        System.out.println("Main constructor");
    }
    
    public Point() {
        this(0.0, 0.0); // 必须第一行
        System.out.println("Default constructor");
    }
    
    public Point(double x) {
        this(x, 0.0);
        System.out.println("Single param constructor");
    }
}
```

#### 示例 3：陷阱（混合初始化）
```cpp
#include <iostream>

class Bad {
    int a_, b_;
public:
    Bad(int a) : a_(a) {}
    
    // 错误！不能同时委托和初始化成员
    // Bad(int a, int b) : Bad(a), b_(b) {} // 编译错误
    
    // 正确：在被委托函数中初始化
    Bad(int a, int b) : Bad(a) {
        b_ = b; // 在函数体赋值
    }
};

int main() {
    Bad b(1, 2);
    return 0;
}
```

---

### 📝 六、分层练习题

#### 🔹 基础题
1. **委托语法**  
   - 为 `Circle` 类添加默认构造函数，委托给 `Circle(double radius)`

2. **执行顺序**  
   - 预测以下代码输出：
     ```cpp
     class C {
         int x_;
     public:
         C(int x) : x_(x) { std::cout << "A"; }
         C() : C(42) { std::cout << "B"; }
     };
     C c;
     ```

#### 🔸 进阶题
3. **代码复用**  
   - 用委托构造减少 `Logger` 类的重复代码

4. **委托链**  
   - 实现 3 层委托：`Point() → Point(x) → Point(x,y)`

#### ⚠️ 陷阱题（Java 迁移误区）
5. **“能在函数体委托吧？”**  
   - Q: C++ 能在构造函数体中调用其他构造函数吗？  
   - A: ❌ **不能**！  
     → **必须在初始化列表中**  
     → Java 可在函数体第一行调用 `this()`

6. **“委托后成员能再初始化吧？”**  
   - Q: `MyClass(int a, int b) : MyClass(a), b_(b) {}` 合法吗？  
   - A: ❌ **不合法**！  
     → **不能同时委托和初始化成员**  
     → 需在函数体赋值 `b_ = b;`

---

### ⚠️ 七、注意事项与建议

| 问题 | 建议 |
|------|------|
| **混合初始化** | 不能同时委托和初始化成员 |
| **无限递归** | 避免直接/间接自委托 |
| **虚函数** | 委托中仍不要调用虚函数 |
| **现代实践** | 用委托减少重复，保持浅链 |

> 💡 **现代 C++ 黄金法则**：  
> **“委托在初始化列表；不能混合初始化；浅委托链更清晰”**

---

### 🔁 八、螺旋复习

- **衔接 2.2.2**：委托构造是初始化列表的扩展  
- **Java 经验**：C++ 委托更早执行（初始化列表 vs 函数体）  
- **当日复习**：默写委托构造的三大规则

---

### 🧠 九、主动 learning 任务

1. **自我提问**：
   - “如何用委托构造处理异常安全？”
   - “委托构造与继承如何交互？”
2. **概念图**：画出 C++ 与 Java 构造器链对比
3. **扮演教师**：向同事解释“为什么委托必须在初始化列表”

---

### 🔍 十、复盘清单

✅ 我能正确使用委托构造  
✅ 我理解执行顺序和限制  
✅ 我会避免混合初始化陷阱  
✅ 我知道与 Java 构造器链的差异  
✅ 我明白现代 C++ 的委托最佳实践

> 🎯 **达标标准**：完成基础题 + 正确分析陷阱题 5 和 6

---

## 🚀 下一步建议

你已掌握 **C++ 委托构造的核心知识**！

接下来你可以：
1. **继续学习 2.2.4 默认与删除构造函数（=default, =delete）**
2. **请求生成「C++ 委托构造速查卡」**
3. **跳转到练习题答案与解析**

请告诉我你的选择，我们将继续前行！