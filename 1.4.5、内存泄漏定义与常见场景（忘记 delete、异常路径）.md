太好了！我们现在进入 **1.4.5：内存泄漏定义与常见场景 — 忘记 delete、异常路径**。

这是 C++ 手动内存管理中**最常见、最隐蔽的风险**。内存泄漏不会立即导致程序崩溃，但会**随时间累积，最终耗尽系统资源**。作为 Java 开发者，你可能不熟悉这种“缓慢死亡”的错误模式。本课将帮你识别泄漏场景，并掌握**RAII 和智能指针**等现代 C++ 防御手段。

我们将继续严格遵循你的学习偏好：
- ✅ SMART 目标  
- ✅ 结构先行（泄漏场景地图）  
- ✅ 生活化比喻  
- ✅ 动手为王（含基础/进阶/陷阱题）  
- ✅ 螺旋复习（衔接 1.4.1–1.4.4 + Java 经验）  
- ✅ 主动学习 + 复盘

---

## 🎯 KU 1.4.5：内存泄漏定义与常见场景

> **所属阶段**：Phase 1 — C++ 语法基础  
> **前置知识**：1.4.1–1.4.4（内存管理全体系）、Java GC 经验  
> **后续衔接**：2.3（RAII）、3.2（智能指针）  
> **预计耗时**：3–4 小时

---

### 🧭 一、结构先行：内存泄漏场景地图

```
1.4.5 内存泄漏
├── 1. 什么是内存泄漏？
│   ├── 1.1 定义：分配的内存无法再访问，但未释放
│   ├── 1.2 与 Java 的对比（GC vs 手动）
│   └── 1.3 泄漏的累积效应
├── 2. 常见泄漏场景
│   ├── 2.1 忘记 delete（最常见）
│   ├── 2.2 早期 return（函数中途退出）
│   ├── 2.3 异常路径（构造函数/函数中抛异常）
│   ├── 2.4 作用域扩大（指针被覆盖）
│   └── 2.5 循环引用（shared_ptr，Phase 3）
├── 3. 检测工具
│   ├── 3.1 Valgrind（Linux/macOS）
│   ├── 3.2 AddressSanitizer（ASan）
│   └── 3.3 Visual Studio Diagnostic Tools（Windows）
├── 4. 防御策略
│   ├── 4.1 RAII（资源获取即初始化）
│   ├── 4.2 智能指针（unique_ptr, shared_ptr）
│   └── 4.3 现代 C++ 最佳实践
└── 5. 与 Java 的根本差异
```

---

### 🎯 二、SMART 学习目标

- **Specific**：能定义内存泄漏，识别 5 种常见场景，并用 RAII/智能指针修复。
- **Measurable**：完成 3 道基础题 + 2 道陷阱题，能用 Valgrind/ASan 检测泄漏。
- **Achievable**：通过“漏水的水桶”比喻理解累积效应。
- **Relevant**：这是编写长期运行 C++ 服务的基础。
- **Time-bound**：2 天内完成。

---

### 🌍 三、生活化比喻：漏水的水桶

> 想象你有一个**水桶（程序）**，每天往里加水（分配内存）：
>
> - **Java = 自动排水系统**  
>   - 水桶有智能传感器，自动排出不用的水（GC）  
>   - 即使你偶尔忘记关水龙头，系统会 later 清理
>
> - **C++ = 手动排水**  
>   - 你必须**自己关水龙头**（`delete`）  
>   - **忘记关** → 水慢慢溢出（内存泄漏）  
>   - **短期无事**，但**长期会淹没房间**（程序 crash）
>
> - **异常路径 = 突然地震**  
>   - 你正要去关水龙头，地震了（抛异常）  
>   - 水龙头一直开着 → 泄漏

> 💡 **关键认知**：**C++ 内存泄漏是静默杀手，需主动防御**。

---

### 📚 四、知识点分解

#### 1. 什么是内存泄漏？
- **定义**：通过 `new`/`malloc` 分配的内存，**失去所有指向它的指针**，且**未释放**。
- **后果**：
  - 程序内存占用持续增长
  - 最终耗尽内存 → `std::bad_alloc` 或系统 kill
- **与 Java 对比**：
  | 特性 | C++ | Java |
  |------|-----|------|
  | **泄漏原因** | 忘记 `delete` | 循环引用、静态集合持有 |
  | **检测时机** | 运行时累积 | GC 时发现不可达 |
  | **修复方式** | 手动配对 | 弱引用、及时清理 |

#### 2. 常见泄漏场景
- **2.1 忘记 `delete`**（最常见）：
  ```cpp
  void f() {
      MyClass* p = new MyClass();
      // 忘记 delete p;
  }
  ```
- **2.2 早期 `return`**：
  ```cpp
  void f(bool cond) {
      MyClass* p = new MyClass();
      if (cond) {
          return; // 忘记 delete p;
      }
      delete p;
  }
  ```
- **2.3 异常路径**：
  ```cpp
  void f() {
      MyClass* p = new MyClass();
      risky_operation(); // 若抛异常，p 泄漏
      delete p;
  }
  ```
- **2.4 指针被覆盖**：
  ```cpp
  void f() {
      MyClass* p = new MyClass();
      p = new MyClass(); // 第一个对象泄漏
      delete p;
  }
  ```
- **2.5 循环引用**（`shared_ptr`，Phase 3）：
  ```cpp
  struct Node {
      std::shared_ptr<Node> next;
      std::shared_ptr<Node> prev;
  };
  // A->B, B->A → 引用计数永不为 0
  ```

#### 3. 检测工具
- **Valgrind**（Linux/macOS）：
  ```bash
  g++ -g leak.cpp -o leak
  valgrind --leak-check=full ./leak
  ```
- **AddressSanitizer**（ASan，跨平台）：
  ```bash
  g++ -fsanitize=address -g leak.cpp -o leak
  ./leak
  ```
- **Visual Studio**：诊断工具 → 内存使用情况

#### 4. 防御策略
- **RAII**（Phase 2）：
  ```cpp
  class SafePtr {
      MyClass* ptr;
  public:
      SafePtr() : ptr(new MyClass()) {}
      ~SafePtr() { delete ptr; } // 自动释放
  };
  ```
- **智能指针**（Phase 3）：
  ```cpp
  void f() {
      auto p = std::make_unique<MyClass>(); // 自动 delete
      risky_operation(); // 即使抛异常，p 也会释放
  }
  ```
- **现代 C++ 最佳实践**：
  - 优先栈对象
  - 其次 `std::unique_ptr`
  - 避免裸 `new`/`delete`

#### 5. 与 Java 的根本差异
- **Java**：泄漏通常是**逻辑错误**（对象意外可达）
- **C++**：泄漏通常是**机械错误**（忘记 `delete`）
- **C++ 优势**：RAII 提供**确定性资源管理**，比 GC 更可靠

---

### 💻 五、动手为王：可运行示例

#### 示例 1：基本泄漏
```cpp
// leak1.cpp
#include <iostream>

class Tracker {
public:
    Tracker(int id) : id_(id) { 
        std::cout << "Construct " << id_ << "\n"; 
    }
    ~Tracker() { 
        std::cout << "Destruct " << id_ << "\n"; 
    }
private:
    int id_;
};

void leak() {
    new Tracker(1); // 泄漏！无指针，无法 delete
    Tracker* p = new Tracker(2);
    // 忘记 delete p;
}

int main() {
    leak();
    std::cout << "Program ends, but memory leaked!\n";
    return 0;
}
```

#### 示例 2：异常路径泄漏
```cpp
// leak2.cpp
#include <iostream>
#include <stdexcept>

void risky() {
    throw std::runtime_error("Oops!");
}

void leak_with_exception() {
    Tracker* p = new Tracker(3);
    risky(); // 抛异常，delete 被跳过
    delete p;
}

int main() {
    try {
        leak_with_exception();
    } catch (...) {
        std::cout << "Caught exception, but memory leaked!\n";
    }
    return 0;
}
```

#### 示例 3：RAII 修复
```cpp
// safe.cpp
#include <iostream>
#include <memory>
#include <stdexcept>

void safe_with_unique_ptr() {
    auto p = std::make_unique<Tracker>(4);
    risky(); // 抛异常，p 自动释放
} // 或正常结束，p 自动释放

int main() {
    try {
        safe_with_unique_ptr();
    } catch (...) {
        std::cout << "Caught exception, no leak!\n";
    }
    return 0;
}
```

---

### 📝 六、分层练习题

#### 🔹 基础题
1. **泄漏识别**  
   - 运行 `leak1.cpp`，用 Valgrind/ASan 检测泄漏

2. **早期 return 修复**  
   - 修复以下代码：
     ```cpp
     void f(bool cond) {
         MyClass* p = new MyClass();
         if (cond) return;
         delete p;
     }
     ```

#### 🔸 进阶题
3. **异常安全**  
   - 用 `std::unique_ptr` 重写 `leak_with_exception`

4. **指针覆盖**  
   - 识别并修复以下泄漏：
     ```cpp
     void f() {
         MyClass* p = new MyClass();
         p = new MyClass();
         delete p;
     }
     ```

#### ⚠️ 陷阱题（Java 迁移误区）
5. **“程序结束会自动释放吧？”**  
   - Q: C++ 程序结束时，操作系统会回收内存，所以泄漏无所谓？  
   - A: ⚠️ **部分正确但危险**：  
     - 操作系统会回收**内存**  
     - **但析构函数不会被调用**！  
     → 若析构中有重要操作（如文件 flush、网络断开），会丢失数据

6. **“Java 也会泄漏，所以 C++ 一样”**  
   - Q: C++ 和 Java 的内存泄漏是一回事吗？  
   - A: ❌ **根本不同**：  
     - Java 泄漏：对象**意外可达**（逻辑错误）  
     - C++ 泄漏：**忘记释放**（机械错误）  
     → C++ 泄漏更易通过 RAII 防御

---

### ⚠️ 七、注意事项与建议

| 问题 | 建议 |
|------|------|
| **裸指针** | 避免裸 `new`/`delete`，用智能指针 |
| **异常安全** | 用 RAII 确保异常路径资源释放 |
| **检测工具** | 开发时启用 ASan（`-fsanitize=address`） |
| **代码审查** | 重点检查裸指针生命周期 |
| **现代替代** | `std::vector`、`std::unique_ptr` 是默认选择 |

> 💡 **现代 C++ 建议**：
> - **新代码**：零裸 `new`/`delete`
> - **遗留代码**：逐步替换为智能指针
> - **系统编程**：用 RAII 封装资源（文件、锁、内存）

---

### 🔁 八、螺旋复习

- **衔接 1.4.2**：`new`/`delete` 配对是防泄漏基础
- **衔接 2.3**：RAII 是 C++ 资源管理的核心范式
- **Java 经验**：C++ 泄漏是机械错误，Java 是逻辑错误
- **当日复习**：默写 5 种泄漏场景

---

### 🧠 九、主动学习任务

1. **自我提问**：
   - “为什么 RAII 能解决异常路径泄漏？”
   - “Valgrind 和 ASan 有什么区别？”
2. **概念图**：画出泄漏场景与防御策略的对应关系
3. **扮演教师**：向同事解释“为什么程序结束时析构函数不被调用”

---

### 🔍 十、复盘清单

✅ 我能定义内存泄漏  
✅ 我能识别 5 种常见泄漏场景  
✅ 我会用 Valgrind/ASan 检测泄漏  
✅ 我知道 RAII/智能指针是防御核心  
✅ 我明白 C++ 与 Java 泄漏的根本差异

> 🎯 **达标标准**：完成基础题 + 正确分析陷阱题 5 和 6

---

## 🚀 下一步建议

你已掌握 **C++ 内存泄漏的核心知识**！

接下来你可以：
1. **继续学习 1.4.6 悬空指针（Dangling Pointer）与野指针（Wild Pointer）**
2. **请求生成「内存泄漏防御检查清单」**
3. **跳转到练习题答案与解析**

请告诉我你的选择，我们将继续前行！