太好了！我们现在进入 **2.1.1：class 与 struct 的等价性与默认访问权限差异**。

这是 **Phase 2：面向对象编程** 的开篇课，也是理解 C++ **统一对象模型**的关键。作为 Java 开发者，你可能认为 `class` 是唯一面向对象的工具，而 C++ 的 `struct` 仅用于数据聚合。但实际上，**C++ 的 `class` 和 `struct` 几乎完全等价**，唯一的区别是**默认访问权限**。掌握这一本质，能让你在设计数据类型时做出更符合 C++ 习惯的选择。

我们将继续严格遵循你的学习偏好：
- ✅ SMART 目标  
- ✅ 结构先行（等价性地图）  
- ✅ 生活化比喻  
- ✅ 动手为王（含基础/进阶/陷阱题）  
- ✅ 螺旋复习（衔接 Phase 1 + Java 经验）  
- ✅ 主动学习 + 复盘

---

## 🎯 KU 2.1.1：class 与 struct 的等价性

> **所属阶段**：Phase 2 — 面向对象编程  
> **前置知识**：Phase 1（C++ 语法基础）、1.6（struct 基础）、Java class 经验  
> **后续衔接**：2.2（构造函数）、2.3（RAII）  
> **预计耗时**：2–3 小时

---

### 🧭 一、结构先行：等价性地图

```
2.1.1 class 与 struct 等价性
├── 1. 核心等价性
│   ├── 1.1 语法等价：仅默认访问权限不同
│   ├── 1.2 功能等价：都支持成员函数、继承、模板等
│   └── 1.3 内存等价：对象布局完全相同
├── 2. 默认访问权限差异
│   ├── 2.1 class：默认 private
│   └── 2.2 struct：默认 public
├── 3. 设计语义与惯例
│   ├── 3.1 struct：数据聚合（POD、简单对象）
│   └── 3.2 class：行为封装（复杂对象、资源管理）
├── 4. 与 Java 的对比
│   ├── 4.1 Java 只有 class
│   └── 4.2 C++ 提供两种语法糖
└── 5. 最佳实践
    ├── 5.1 语义选择 > 语法限制
    ├── 5.2 新代码：struct 用于数据，class 用于行为
    └── 5.3 避免混用（保持团队一致性）
```

---

### 🎯 二、SMART 学习目标

- **Specific**：能解释 `class` 与 `struct` 的等价性，理解默认访问权限差异，并根据语义选择合适的关键字。
- **Measurable**：完成 2 道基础题 + 2 道陷阱题，能修复因默认权限导致的编译错误。
- **Achievable**：通过“透明盒子 vs 封装盒子”比喻理解设计语义。
- **Relevant**：这是编写符合 C++ 习惯的面向对象代码的基础。
- **Time-bound**：1–2 天内完成。

---

### 🌍 三、生活化比喻：透明盒子 vs 封装盒子

> 回想 Phase 1 的比喻（1.6.2）：
>
> - **struct = 透明盒子**  
>   - 默认内容**公开可见**（public）  
>   - 适合**数据展示**（Point, Config）
>
> - **class = 封装盒子**  
>   - 默认内容**隐藏**（private）  
>   - 适合**行为封装**（FileHandle, Logger）
>
> - **关键认知**：  
>   - **盒子材质相同**（功能等价）  
>   - **仅默认门锁不同**（访问权限）
>
> - **Java 对比**：  
>   - Java **只有封装盒子**（class）  
>   - **C++ 提供两种盒子**，让你按需选择

> 💡 **关键认知**：**`class` 和 `struct` 是同一枚硬币的两面 —— 选择基于设计语义，而非功能**。

---

### 📚 四、知识点分解

#### 1. 核心等价性
- **语法等价**：
  ```cpp
  // 完全等价！
  struct Point { double x, y; };
  class Point { public: double x, y; };
  ```
- **功能等价**：
  - 都支持：成员函数、构造函数、继承、虚函数、模板特化
  ```cpp
  struct MyStruct {
      void method();           // 成员函数
      virtual ~MyStruct();     // 虚析构
  };
  
  class MyClass {
  public:
      void method();           // 成员函数
      virtual ~MyClass();      // 虚析构
  };
  ```
- **内存等价**：
  - 相同成员的 `class` 和 `struct` 有**完全相同的内存布局**
  - `sizeof(MyClass) == sizeof(MyStruct)`

#### 2. 默认访问权限差异
| 关键字 | 默认成员访问 | 默认继承权限 |
|--------|-------------|------------|
| `class` | **private** | **private** |
| `struct` | **public** | **public** |

- **示例**：
  ```cpp
  class C {
      int x; // private by default
  };
  
  struct S {
      int x; // public by default
  };
  
  class DerivedC : C {}; // private inheritance
  struct DerivedS : S {}; // public inheritance
  ```

#### 3. 设计语义与惯例
- **`struct` 惯例**：
  - **数据为主**，行为为辅
  - 成员通常 public（如 `Point`, `Config`）
  - POD（Plain Old Data）类型
- **`class` 惯例**：
  - **行为为主**，数据隐藏
  - 成员通常 private，通过 public 方法访问
  - 封装资源（如 `FileHandle`, `Mutex`）

#### 4. 与 Java 的对比
| 特性 | C++ | Java |
|------|-----|------|
| **类型关键字** | `class` 和 `struct` | 仅 `class` |
| **默认访问** | class: private<br>struct: public | package-private |
| **设计语义** | struct = 数据<br>class = 封装 | class = 通用 |
| **内存布局** | 开发者可控 | JVM 隐藏 |

#### 5. 最佳实践
- **语义选择**：
  - **数据聚合** → `struct`
  - **行为封装** → `class`
- **新代码**：
  - 优先按语义选择，而非功能（因功能等价）
- **团队一致性**：
  - 避免在同一项目中混用语义（如用 `class` 表示 Point）

> 💡 **现代 C++ 黄金法则**：  
> **“用 struct 表示数据，用 class 表示行为；功能等价，语义不同”**

---

### 💻 五、动手为王：可运行示例

#### 示例 1：功能等价性验证
```cpp
#include <iostream>

// struct with members
struct PointS {
    double x, y;
    double distance() const {
        return x*x + y*y;
    }
};

// class with explicit public
class PointC {
public:
    double x, y;
    double distance() const {
        return x*x + y*y;
    }
};

int main() {
    PointS ps{3.0, 4.0};
    PointC pc{3.0, 4.0};
    
    std::cout << "Struct distance: " << ps.distance() << "\n";
    std::cout << "Class distance: " << pc.distance() << "\n";
    
    // 内存布局相同
    std::cout << "Size struct: " << sizeof(ps) << "\n";
    std::cout << "Size class: " << sizeof(pc) << "\n";
    
    return 0;
}
```

#### 示例 2：默认权限差异
```cpp
#include <iostream>

class MyClass {
    int secret = 42; // private by default
public:
    void reveal() { std::cout << "Secret: " << secret << "\n"; }
};

struct MyStruct {
    int secret = 42; // public by default
};

int main() {
    MyClass c;
    c.reveal(); // OK
    
    MyStruct s;
    std::cout << "Struct secret: " << s.secret << "\n"; // OK (public)
    
    // c.secret; // Error! private
    return 0;
}
```

#### 示例 3：继承权限差异
```cpp
#include <iostream>

class BaseC {
public:
    void f() { std::cout << "BaseC::f\n"; }
};

struct BaseS {
    void f() { std::cout << "BaseS::f\n"; }
};

class DerivedC : BaseC {}; // private inheritance
struct DerivedS : BaseS {}; // public inheritance

int main() {
    DerivedC dc;
    // dc.f(); // Error! private inheritance
    
    DerivedS ds;
    ds.f(); // OK! public inheritance
    
    return 0;
}
```

---

### 📝 六、分层练习题

#### 🔹 基础题
1. **等价性验证**  
   - 创建 `struct Point` 和等价的 `class Point`，验证功能相同

2. **默认权限**  
   - 解释以下代码的访问权限：
     ```cpp
     class C { int x; };
     struct S { int x; };
     ```

#### 🔸 进阶题
3. **继承权限**  
   - 比较 `class D : C {}` 和 `struct D : C {}` 的继承权限

4. **语义选择**  
   - 为以下场景选择 `struct` 或 `class`：  
     a) 2D 点坐标  
     b) 文件读写器  
     c) HTTP 请求配置

#### ⚠️ 陷阱题（Java 迁移误区）
5. **“struct 不能有方法吧？”**  
   - Q: C++ `struct` 能包含成员函数吗？  
   - A: ✅ **能**！  
     → `struct` 和 `class` **功能完全等价**

6. **“class 更强大吧？”**  
   - Q: C++ `class` 比 `struct` 功能更强吗？  
   - A: ❌ **不**！  
     → **唯一差异是默认访问权限**  
     → 一切功能均可通过显式 `public`/`private` 实现

---

### ⚠️ 七、注意事项与建议

| 问题 | 建议 |
|------|------|
| **语义混淆** | 按数据/行为选择，而非功能 |
| **继承权限** | 显式写 `public`/`private` 避免默认 |
| **POD 类型** | 用 `struct` 表示 C 兼容数据 |
| **团队规范** | 统一项目风格（如 Google C++ Style） |
| **现代实践** | 新代码按语义选择关键字 |

> 💡 **现代 C++ 黄金法则**：  
> **“struct = 数据，class = 行为；功能等价，语义先行”**

---

### 🔁 八、螺旋复习

- **衔接 1.6**：Phase 1 已介绍 struct 基础
- **Java 经验**：C++ 提供更精细的类型语义
- **当日复习**：默写 class/struct 的默认权限表

---

### 🧠 九、主动学习任务

1. **自我提问**：
   - “为什么 C++ 保留 struct 和 class 两种关键字？”
   - “能否用 class 完全替代 struct？”
2. **概念图**：画出 class 与 struct 的等价性对比
3. **扮演教师**：向同事解释“为什么 Point 用 struct 而 FileHandle 用 class”

---

### 🔍 十、复盘清单

✅ 我能说出 class 与 struct 的核心等价性  
✅ 我理解默认访问权限差异  
✅ 我会按语义选择合适关键字  
✅ 我知道与 Java 的设计差异  
✅ 我明白现代 C++ 的最佳实践

> 🎯 **达标标准**：完成基础题 + 正确分析陷阱题 5 和 6

---

## 🚀 下一步建议

你已掌握 **C++ class 与 struct 等价性的核心知识**！

接下来你可以：
1. **继续学习 2.1.2 成员函数定义：类内 vs 类外**
2. **请求生成「C++ class vs struct 速查卡」**
3. **跳转到练习题答案与解析**

请告诉我你的选择，我们将继续前行！